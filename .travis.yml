language: php

php:
    - 7.1

sudo: false

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction

services:
  - memcached
  - redis-server

script:
  - php artisan key:generate
  - vendor/bin/phpunit
